let Airplane = {};
Airplane.myAirplane = "StarJet";

module.exports = Airplane;

/* In order to export a module an object must first be defined to represent the
module. After defining the object, add properties or behaviours to it. Export the
module using 'module' which is a variable that represents the module and 'exports'
which exposes the module as an object and set equal to the name of the
object/module created. */
