const numberClusters = [[1, 2], [3, 4], [5, 6]];

const target = numberClusters[2][1];

/* Arrays can hold any type of data as an element including other arrays. This
is called a nested array. Nested arrays can also be accessed using bracket
notation. To access the elements withing the nested array we can chain more
bracket notation with index values. */
