const hobbies = ['Reading', 'Dancing', 'Writing Music'];

console.log(hobbies);

/* One way to create an array is by creating an array literal. This id done
by wrapping items within square brackets. Each item inside an array is called an
element. Arrays can hold various data types and they can be mixed within the
same array. */
