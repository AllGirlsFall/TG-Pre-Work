const chores = ['wash dishes', 'do laundry', 'take out trash', 'cook dinner', 'mop floor'];

chores.pop();
console.log(chores);

/* Another method for arrays is the pop method. This method removes the last item
from the array. It does not take any arguments and is also considered a
mutative method since it changes the array. */
