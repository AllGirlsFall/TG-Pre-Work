const objectives = ['Learn a new languages', 'Read 52 books', 'Run a marathon'];

console.log(objectives.length); // output is 3

/* Arrays also have their own built-in properties and one such property is length.
The length property tells you how many elements are in array. It is used with
dot notation and appended to the array name. */
