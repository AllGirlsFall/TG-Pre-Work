let newYearsResolutions = ['Keep a journal', 'Take a falconry class', 'Learn to juggle'];

console.log(newYearsResolutions);

/* Arrays are the way that JavaScript makes lists. They can store any type of
data even other arrays in the same array. */
