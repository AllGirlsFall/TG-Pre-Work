const chores = ['wash dishes', 'do laundry', 'take out trash'];

chores.push('walk the dog', 'vacuum');
console.log (chores);

/* Arrays have built in methods to make arrays easier to work with. One such
method is call push and it adds items onto the end of an array. It is known as
destructive method as it changes the initial array. */
