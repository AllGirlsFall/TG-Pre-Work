let newVariable = 'Playing around with typeof.';
console.log(typeof newVariable);

newVariable = 1;
console.log(typeof newVariable);

/* The typeOf operator is useful to track data types when writing code. This operator
checks the value to the right of it and returns a string telling you what type
of data the value is. */
