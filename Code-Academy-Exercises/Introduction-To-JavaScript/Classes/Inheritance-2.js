class HospitalEmployee {
  constructor(name) {
    this._name = name;
    this._remainingVacationDays = 20;
  }

  get name() {
    return this._name;
  }

  get remainingVacationDays() {
    return this._remainingVacationDays;
  }

  takeVacationDays(daysOff) {
    this._remainingVacationDays -= daysOff;
  }
}

/* The above code has shared methods and properties that have been abstracted into
a parent class of HospitalEmployee for two child classes of Surgeon and Nurse (not
yet shown). */
