/* When multiple classes share properties or methods become candidates for
inheritance. Using inheritance, you can create a parent class (superclass) with
properties and methods that multiple child classes (subclass) share. Child
classes inherit these properties and methods from their parent class. */
