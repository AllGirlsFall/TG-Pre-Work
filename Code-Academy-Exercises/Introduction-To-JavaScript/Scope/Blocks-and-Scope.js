const city = 'New York City';

const logCitySkyline = () => {
  let skyscraper = 'Empire State Building';
  return 'The stars over the ' + skyscraper + ' in ' + city;
};

console.log(logCitySkyline());

/* Blocks are the code found in a set of curly brackets and help us group one or
more statements together. When a variable is written inside the block of code it
is only accessible to that function or locally. */
