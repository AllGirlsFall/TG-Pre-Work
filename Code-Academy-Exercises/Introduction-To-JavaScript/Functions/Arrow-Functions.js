const plantNeedsWater = (day) => {
  if (day === 'Wednesday') {
    return true;
  } else {
    return false;
  }
};

/* Arrow function syntax was introduced in ES^ and is a shorter way to write a
function. They remove the need to type out  'function' and use the "fat arrow" (=>)
instead. The parameters are still used along with the curly braces that contain
the block of code to be executed. *?
