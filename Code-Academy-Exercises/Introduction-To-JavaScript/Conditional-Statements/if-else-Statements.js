let sale = true;
sale = false;

if(sale) {
  console.log('Time to buy!');
} else {
  console.log('Time to wait for sale.');
}

/* In order to execute a different block of code when the condition evaluates to
false we can use the 'else' statement to our code. After the first block of code
else is added and then a new block of code in curly brackets. */
