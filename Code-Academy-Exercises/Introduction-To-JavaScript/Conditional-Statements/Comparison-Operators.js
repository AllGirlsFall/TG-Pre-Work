let hungerLevel = 7;

if (hungerLevel > 7) {
  console.log('Time to eat!');
} else {
  console.log('We can eat later!');
}

/* Comparison operators compare the values in the conditional statement or the
values on the left to the values on the right. Some conditional operators other
than the greater-than symbol shown in the example are: less than,
less than or equal to, greater than or equal to, equal to which uses the identity
operator and not equal to. */
