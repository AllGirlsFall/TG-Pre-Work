let cupsOfSugarNeeded = 3;
let cupsAdded = 0;

do {
  cupsAdded++
} while (cupsAdded < cupsOfSugarNeeded);

/* The do while statement is useful for when you want a block of code to execute
at least once (whether or not it's true) and to keep executing until the condition
is no longer true. */
