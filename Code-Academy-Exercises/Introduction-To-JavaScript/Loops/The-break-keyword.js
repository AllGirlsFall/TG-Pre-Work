for (let i = 0; i < rapperArray.length; i++) {
console.log(rapperArray[i]);
  if (rapperArray[i] === 'Notorious B.I.G.'){
  break;
}
}
console.log("And if you don't know, now you know.")

/* The break keyword is used for when you want a loop to stop iterating even
though the stopping condition hasn't been met. It allows the program to break out
of the loop from within the loop's block. */
