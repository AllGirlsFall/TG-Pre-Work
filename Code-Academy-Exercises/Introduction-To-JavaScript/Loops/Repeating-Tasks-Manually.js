const vacationSpots = ['Los Angeles', 'Japan', 'Seoul'];
console.log(vacationSpots[0]);
console.log(vacationSpots[1]);
console.log(vacationSpots[2]);

/* This is a manual example of how we would write code if we didn't have
loops to use. You would have to go through each element of the array and log
it to the console which would be tedious and time consuming. */
