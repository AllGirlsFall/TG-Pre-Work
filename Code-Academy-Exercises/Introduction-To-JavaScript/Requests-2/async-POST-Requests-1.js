/* Like the other GET POST requests, the async POST request requires a little
more information to process. In the fetch function we will need to define the
method and body before the resonse can be handled correctly. */
