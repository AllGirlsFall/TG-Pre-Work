/* An overview showing the boilerplate of a POST request using the fetch
function. The initial call takes 2 arguments: an endpoint and the object
with the information needed for the post request. The rest of the request is
just like the GET request. */
